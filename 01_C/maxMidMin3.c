#include <stdio.h>

int main(void)
{
	int a, b, c;
	scanf("%d %d %d", &a, &b, &c);

	if (a > b){
		if (c > a) // cab
		else if (c > b) //acb
		else //abc
	} else
		if (c > b) //cba
		else if (c > a) //bca
		else //bac
	}

	return 0;
}
